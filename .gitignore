# build output
dist/
dist.zip
*.zip

# raw image masters (client-supplied PNG/JPEG originals). The optimised .webp
# derivatives in public/images are committed and are what the site serves;
# these are kept locally only so a source file can be re-encoded if needed.
source-images/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
